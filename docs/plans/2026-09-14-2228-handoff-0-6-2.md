status: accepted

# Handoff — 0.6.2, o molde do plano sem caminho de home — 2026-09-14 22:28

> Leia isto primeiro. Supera `2026-09-14-1853-handoff-readme-em-duas-linguas.md`, cujo único limite
> aberto é o que esta frente fecha. Todo número vem com o comando rodado no ato.

## Estado (medido no ato)

| Repositório | Ramo | Base da frente | Versão | Cópia instalada |
|---|---|---|---|---|
| `roadworthy` | `main` | `c3ee35c` (igual a `origin/main` ao abrir) | 0.6.2 nos dois manifests e no CHANGELOG | **ainda 0.6.1** (`installed_plugins.json`: `installPath …/0.6.1`, `gitCommitSha 531f8f3`) até push + `claude plugin update roadworthy@roadworthy` + `/reload-plugins` |

## O que esta frente fez

O dono perguntou por que o limite do molde ficou aberto na frente anterior em vez de ser
consertado; a resposta certa era consertar. Pedido: "vai".

- `skills/plan/templates/plan.md` — a linha `project:` ensina `~/…`, com a razão no parágrafo
  abaixo: o portão expande `~` (`hooks/plan-review-gate`, função `same`, desde a 0.5.0 por
  `git log -S'expanduser'`), e um caminho absoluto de home num arquivo commitado publica o desenho
  da máquina. Medido antes: a varredura de privacidade da suíte reprovou o plano da frente do README
  por esse caminho; o plano em modo de plano desta sessão, com `~`, foi eleito e aprovado pelo portão.
- `tests/scripts/plan-template.sh` — duas asserções: a linha começa com `~/`; nenhum `/Users/` ou
  `/home/` no molde. Escritas antes do conserto: vermelhas no molde antigo (`project line does not
  start with ~/`), verdes no novo, refutadas com o defeito plantado (hash conferido).
- Portão novo em `.roadworthy/gates`: `plugin.json`, `marketplace.json` e o CHANGELOG concordam na
  versão. Ninguém verificava isso. Refutado duas vezes (marketplace uma versão atrás; CHANGELOG sem
  a data no cabeçalho), vermelho com `version-parity:`, verde no arquivo limpo.
- `CHANGELOG.md` — `[0.6.2]` com o `[Unreleased]` do README dentro; `.claude-plugin/plugin.json` e
  `.claude-plugin/marketplace.json` em 0.6.2.

## Por que virou versão

O molde é lido pelo agente da cópia instalada, um diretório por versão no cache
(`~/.claude/plugins/cache/roadworthy/roadworthy/<versão>/`). Sem versão nova o conserto só
existiria no repositório. O README de hoje não é lido em tempo de execução por hook nem skill, por
isso tinha ficado em `[Unreleased]`; sobe junto agora.

## Onde o estado real mora

- `CHANGELOG.md`, `[0.6.2]`.
- `docs/plans/done/2026-09-14-2226-molde-do-plano-sem-caminho-de-home.md` — o plano, com aceite,
  correções declaradas e os dois portões.
- `.roadworthy/gates` — oito portões: os sete de antes e a paridade de versão.

## Placar — o que esta frente errou no caminho

| classe | o que foi | defeito |
|---|---|---|
| Limite declarado em vez de conserto medível (frente anterior) | deixei o molde para "quando você quiser" com a frente já fechando; a regra da casa diz que decisão que eu posso medir é minha; o dono cobrou e tinha razão | processo |
| Procurei o arquivo, não a forma (leitor frio, rodada 1, bloqueio) | a varredura grepou `templates/plan.md` e "absolute path" e não `project:`; a negação do próprio portão (`hooks/plan-review-gate`, "belongs to another project") ensinava `project: /Users/…`, o oposto do molde novo. Consertada: a dica sai a partir da home (`~/…`); duas asserções novas no caso do portão (a dica diz `~`; um plano com `project: ~/…` é eleito), a segunda medindo o que o CHANGELOG afirmava sem prova; ambas refutadas com o defeito plantado. A classe: "ninguém mais produz X" exige grep pela FORMA no repositório inteiro | `hooks/plan-review-gate:224` |
| O plano carregava a string exata da injeção | a linha de refutação do plano citava o caminho de home literal e a varredura de privacidade leu o plano; a citação ficou sem a home literal, e a string exata mora no registro da refutação | `tests/meta/privacy.sh` |

## Próximo passo concreto

1. `git push` (dono, YubiKey) → `gh run list --limit 3` verde.
2. `claude plugin update roadworthy@roadworthy` + `/reload-plugins` → `claude plugin details` deve
   dizer 0.6.2, e `skills/plan/templates/plan.md` na cópia instalada deve começar `project: ~/`.
3. O primeiro uso real noutro projeto, como o roteiro já dizia.

## Prompt para colar

```
Roadworthy 0.6.2: leia docs/plans/2026-09-14-2228-handoff-0-6-2.md ANTES de agir, depois a seção
[0.6.2] do CHANGELOG e o roteiro. Confira no ato: git status, close.sh --check, gh run list --limit 3,
claude plugin details roadworthy@roadworthy. Aberto: nada desta frente; próximo é o primeiro uso real
noutro projeto.
```

## Painel de fechamento — 2026-09-14 22:49 (hora do `close.sh`)

1. **O que mudou para o dono:** o molde do plano e a negação do portão ensinam a mesma forma,
   `project: ~/…`, e um plano commitado dentro do repositório deixa de reprovar a varredura de
   privacidade; a versão do plugin passou a ter portão; 0.6.2 pronta para publicar.
2. **Entregue, por caminho:** `skills/plan/templates/plan.md`, `hooks/plan-review-gate`,
   `tests/scripts/plan-template.sh`, `tests/hooks/plan-review-gate.sh`, `.roadworthy/gates`
   (oitavo portão, paridade de versão), `CHANGELOG.md` (`[0.6.2]`), `.claude-plugin/plugin.json`,
   `.claude-plugin/marketplace.json`, `docs/plans/done/2026-09-14-2226-molde-do-plano-sem-caminho-de-home.md`
   com linha em `docs/plans/done/README.md`, `docs/plans/2026-09-14-1853-handoff-readme-em-duas-linguas.md`
   (status), este handoff.
3. **Próximo a entregar e o que consome deste:** o push (dono, YubiKey) consome os commits
   `a0ce038` e o de fechamento; a CI consome `tests/run.sh`; `claude plugin update` consome os
   manifests em 0.6.2; `/reload-plugins` consome a cópia instalada.
4. **Prova, comando rodado no ato → saída:** `bash skills/close/scripts/close.sh` em `4586ee1`
   (árvore `bee149ab4ec26c75`) → `close: passed`, 8 portões OK; `bash tests/run.sh` → `RESULT: gate
   clean`; `bash tests/attack.sh` → `every cheat refused, every pass declared`; `docs-check: OK`;
   `pointers-check: OK`; paridade dos READMEs exit 0; paridade de versão exit 0; `claude plugin
   validate . --strict` → `Validation passed`; `plan-preflight.sh --closing` → verde (4 correções
   feitas, 7 arquivos lidos inteiros). **Teste que reprova antes do conserto:** `plan-template.sh` →
   `project line does not start with ~/` no molde antigo; `plan-review-gate.sh` → `the denial
   hints an absolute home path` no portão antigo. **Contraprovas que refutam (5 registros):** molde
   com caminho de home plantado; marketplace uma versão atrás; CHANGELOG sem a data; a dica do
   portão de volta à raiz absoluta; a expansão de `~` removida de `same` — cada uma vermelha com o
   texto esperado, verde no arquivo limpo, hash conferido. Leitor frio sobre o diff: rodada 1
   REJECTED (1 bloqueio, 1 não verificado), rodada 2 APPROVED (zero, zero).
5. **Dentro da tolerância?** Sim: dentro do teto de duas rodadas; o bloqueio da rodada 1 era da
   minha varredura (placar).
6. **Números antes → depois:** portões declarados 7 → 8; asserções do molde 2 → 4; asserções do
   portão +2; ocorrências de "project: <caminho absoluto>" ensinadas pelo plugin 2 → 0; versão
   0.6.1 → 0.6.2; refutações desta frente 0 → 5.
7. **Acervo tocado:** 8 arquivos do plugin, +66/−6; criados: este handoff, o plano; movido: o
   plano de `docs/plans/` para `done/` (mesmo conteúdo salvo `status:`); apagado: nada.
8. **Limite ainda aberto:** push, `plugin update` e `/reload-plugins` são atos do dono; a CI só
   se mede depois do push.
9. **Decisão do dono:** `! git push`; depois `claude plugin update roadworthy@roadworthy` e
   `/reload-plugins`.
